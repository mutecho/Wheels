# Work Items

## Active Follow-up

- run authoritative O2Physics configure/build/ctest
- validate real-data `build-cf` equivalence against the legacy macro
- inspect fit stability on real slices

## Deferred Future Work

- optional unlike-sign support
- optional source-model selection
- optional fit-range and parameter-limit tuning after real-data feedback
